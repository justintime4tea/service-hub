const ServiceHub = require('../../index');
/* eslint-disable no-unused-expressions */
describe('ServiceHub', () => {
  it('should have a constructor', () => {
    ServiceHub.prototype.constructor.should.exist;
  });
});
