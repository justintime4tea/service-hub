/**
 * A semantically versioned provider/consumer system.
 */
class ServiceHub {
  /**
   * Returning to this once we have a way of tracking semantically
   * versioned consumers and providers.
   *
   * Another library is being created to do just that so we can use
   * it in other projects as well.
   **/
}

module.exports = ServiceHub;
